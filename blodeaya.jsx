import { StatusBar } from "expo-status-bar";
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  TouchableOpacity,
  Touchable,
  ImageBackground,
  Image,
} from "react-native";

export default function Note1() {
  return;
}
const styles = StyleSheet.create({});
